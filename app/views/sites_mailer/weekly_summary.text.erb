Your Weekly Analytics Summary

Hi <%= @user.email %>,

Here's your weekly analytics summary for the sites you're a member of.

<% @sites_data.each do |site_data| %>
================================================================================
<%= site_data[:site].name %>
================================================================================

Metrics:
  Daily Avg Pageviews:  <%= number_with_precision(site_data[:avg_daily_pageviews], precision: 1) %>
  Daily Avg Sessions:   <%= number_with_precision(site_data[:avg_daily_sessions], precision: 1) %>
  Daily Avg Duration:   <% if site_data[:avg_daily_duration] %><%= distance_of_time_in_words(site_data[:avg_daily_duration]) %><% else %>N/A<% end %>

<% if site_data[:top_page_by_sessions] || site_data[:top_page_by_duration] %>
Top Performing Pages:
<% end %>

<% if site_data[:top_page_by_sessions] %>
  Most Sessions:
    <%= site_data[:top_page_by_sessions][:pathname] %>
    <%= site_data[:top_page_by_sessions][:sessions] %> sessions

<% end %>
<% if site_data[:top_page_by_duration] %>
  Longest Avg Duration:
    <%= site_data[:top_page_by_duration][:pathname] %>
    <%= distance_of_time_in_words(site_data[:top_page_by_duration][:avg_duration]) %>

<% end %>
<% if site_data[:avg_daily_pageviews].zero? && site_data[:avg_daily_sessions].zero? %>
⚠️  No analytics data available for the past 7 days. Your tracking script may not be set up yet.

<% end %>
<% end %>

View Dashboard: <%= sites_url %>

Questions? Check out the TinyPixel documentation at https://tinypixel.tech/docs
