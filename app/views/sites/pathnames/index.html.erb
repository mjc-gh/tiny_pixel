<turbo-frame id="pathname_stats">
  <%= render PathnameSummaryComponent.new(
    stats: @stats,
    pagination: @stats,
    frame_id: "pathname_stats",
    base_path: site_pathnames_path(@site),
    params: { interval: current_interval }
  ) %>
</turbo-frame>
