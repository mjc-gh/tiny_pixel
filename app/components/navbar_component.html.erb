<nav class="fixed top-0 left-0 right-0 bg-white border-b border-gray-200 z-50">
  <div class="container mx-auto px-5">
    <div class="flex justify-between items-center h-16">
      <%= link_to "tiny_pixel", root_path, class: "font-bold text-xl text-gray-900" %>

      <div class="flex items-center space-x-6">
        <% if signed_in? %>
          <%= link_to "Sites", sites_path, class: "text-gray-600 hover:text-gray-900" %>
          <%= link_to "Profile", profile_path, class: "text-gray-600 hover:text-gray-900" %>
          <%= link_to "Logout", logout_path, data: { turbo_method: :delete }, class: "text-gray-600 hover:text-gray-900" %>
        <% else %>
          <%= link_to "Login", login_path, class: "text-gray-600 hover:text-gray-900" %>
          <%= link_to "Sign Up", sign_up_path, class: "text-gray-600 hover:text-gray-900" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
