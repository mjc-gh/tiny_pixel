<div class="p-4 border rounded">
  <h2 class="font-bold text-lg"><%= @site.name %></h2>
  <p class="text-sm text-gray-600"><%= @site.property_id %></p>
  <p class="text-sm text-gray-500">Created <%= @site.created_at.strftime("%B %d, %Y") %></p>
</div>
