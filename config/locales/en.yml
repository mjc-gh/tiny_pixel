# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

en:
  hello: "Hello world"
  common:
    save: "Save"
    back: "Back"
    settings: "Settings"
  visitors:
    browsers:
      chrome: "Chrome"
      edge: "Edge"
      safari: "Safari"
      firefox: "Firefox"
      opera: "Opera"
      other: "Other"
    device_types:
      desktop: Desktop
      mobile: Mobile
      crawler: Web Crawler
      other: Other
    unknown_value: Unknown
  sites:
    edit:
      title: "Site Settings"
      settings: "Settings"
      salt_duration_label: "Salt Cycling Frequency"
      salt_duration_help: "Controls how often the site's visitor salt is rotated"
      display_hostname_label: "Display Hostname"
      display_hostname_help: "When enabled, stats are grouped by hostname and pathname. When disabled, stats are grouped by pathname only."
    salt_durations:
      daily: "Daily"
      weekly: "Weekly"
      monthly: "Monthly"
    update:
      success: "Site settings updated successfully"
    instructions:
      title: "Setup Instructions"
      description: "Add this script tag to your website to start tracking visitors."
      snippet_label: "Tracking Script"
      copy: "Copy to clipboard"
      close: "Close"

